#include<bits/stdc++.h>
using namespace std;

#define bitsize 10

//for gcc compiler computation

int main()
{

	freopen("inputf.in", "r", stdin);
	freopen("outputf.in", "w", stdout);

	/*cout << bitset<bitsize>(-123) << endl;
	int x = 123;

	int ans = (1 << 31) - x;
	cout << bitset<bitsize>(ans) << endl;
	cout << (~x) << endl;
*/
	
	
	return 0;
}